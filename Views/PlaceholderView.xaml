<UserControl x:Class="stockfront.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:stockfront.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PlaceholderViewModel}"
             FontFamily="Segoe UI">

    <Grid Margin="32,28">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding Title}" FontSize="26" FontWeight="Bold"
                       HorizontalAlignment="Center" Foreground="{StaticResource HeadingBrush}" />
            <TextBlock Text="Раздел в разработке" FontSize="14" Margin="0,8,0,0"
                       HorizontalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" />
        </StackPanel>
    </Grid>
</UserControl>
