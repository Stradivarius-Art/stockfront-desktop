using System.Windows.Controls;

namespace stockfront.Views;

/// <summary>The warehouse screen: stock table with filters/search and the receive/write-off forms.</summary>
public partial class WarehouseView : UserControl
{
    public WarehouseView() => InitializeComponent();
}