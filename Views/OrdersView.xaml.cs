using System.Windows.Controls;

namespace stockfront.Views;

/// <summary>The orders screen: the order list with a status filter, CSV export and role-gated actions.</summary>
public partial class OrdersView : UserControl
{
    public OrdersView() => InitializeComponent();
}