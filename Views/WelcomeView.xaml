<Window x:Class="stockfront.Views.WelcomeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:stockfront.ViewModels"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:WelcomeViewModel}"
        Title="StockFront"
        Width="420" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WorkAreaBrush}"
        FontFamily="Segoe UI">

    <!-- Centered card -->
    <Border Background="{StaticResource CardBrush}"
            CornerRadius="20" Margin="28"
            Padding="36,40">
        <Border.Effect>
            <DropShadowEffect Color="#22000000" BlurRadius="24" ShadowDepth="0" />
        </Border.Effect>

        <StackPanel>
            <!-- Brand -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Border Background="{StaticResource AccentBrush}" Width="44" Height="44" CornerRadius="11">
                    <iconPacks:PackIconMaterial Kind="PackageVariantClosed" Foreground="White"
                                                Width="22" Height="22"
                                                HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="StockFront" FontSize="22" FontWeight="Bold"
                               Foreground="{StaticResource HeadingBrush}" />
                    <TextBlock Text="складской учёт" FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}" />
                </StackPanel>
            </StackPanel>

            <TextBlock Text="Добро пожаловать" FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource HeadingBrush}"
                       HorizontalAlignment="Center" Margin="0,28,0,4" />
            <TextBlock Text="Войдите в систему или создайте учётную запись"
                       Foreground="{StaticResource SecondaryTextBrush}" FontSize="13"
                       TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,28" />

            <!-- Primary: sign in -->
            <Button Content="Вход" Command="{Binding LoginCommand}" IsDefault="True"
                    Height="46" Foreground="White" FontSize="15" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" CornerRadius="12" Background="{StaticResource AccentBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Secondary: register -->
            <Button Content="Регистрация" Command="{Binding RegisterCommand}"
                    Height="46" Margin="0,12,0,0" Foreground="{StaticResource AccentBrush}"
                    FontSize="15" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" CornerRadius="12" Background="Transparent"
                                BorderBrush="{StaticResource AccentBrush}" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{StaticResource ActiveNavBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Border>
</Window>
