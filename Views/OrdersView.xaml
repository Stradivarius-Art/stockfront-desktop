<UserControl x:Class="stockfront.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:stockfront.ViewModels"
             xmlns:conv="clr-namespace:stockfront.Converters"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:OrdersViewModel}"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <!-- Carries the ViewModel into a resource so the (visual-tree-less) ColumnDefinitions can
             reach ShowActions to size the role-dependent actions column. -->
        <conv:BindingProxy x:Key="VmProxy" Data="{Binding}"/>

        <Style x:Key="ColHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="Cell" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource HeadingBrush}"/>
        </Style>

        <!-- Outline (secondary) button — used by Export and the row cancel action -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{StaticResource HeadingBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" CornerRadius="12" Padding="22,0" Background="White"
                                BorderBrush="{StaticResource DividerBrush}" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{StaticResource WorkAreaBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small orange action button for the next lifecycle step -->
        <Style x:Key="RowAdvanceButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" CornerRadius="9" Padding="14,0" Background="{StaticResource AccentBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small outline action button for the row cancel -->
        <Style x:Key="RowCancelButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" CornerRadius="9" Padding="12,0" Background="White"
                                BorderBrush="{StaticResource DividerBrush}" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#F8D7D7"/>
                                <Setter Property="Foreground" Value="#D24B4B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Item template: also used for the closed selection box, so the label renders instead of the
             record's ToString() under the custom ComboBox template. -->
        <DataTemplate x:Key="ComboItemLabel">
            <TextBlock Text="{Binding Label}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
        </DataTemplate>

        <!-- Rounded dropdown item — matches the rest of the rounded UI -->
        <Style x:Key="RoundedComboItem" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="Foreground" Value="{StaticResource HeadingBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" CornerRadius="8" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource WorkAreaBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource ActiveNavBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded pill filter combo (closed control + popup) -->
        <Style x:Key="FilterCombo" TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource HeadingBrush}"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource DividerBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource RoundedComboItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="Toggle" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Bd" CornerRadius="18"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{StaticResource DividerBrush}" BorderThickness="1">
                                            <iconPacks:PackIconMaterial Kind="ChevronDown" Width="14" Height="14"
                                                                        HorizontalAlignment="Right" VerticalAlignment="Center"
                                                                        Margin="0,0,14,0"
                                                                        Foreground="{StaticResource SecondaryTextBrush}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              Margin="14,0,36,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>

                            <Popup x:Name="PART_Popup" Placement="Bottom" AllowsTransparency="True" Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Slide">
                                <Border CornerRadius="10" Background="White" Margin="0,4,0,6"
                                        BorderBrush="{StaticResource DividerBrush}" BorderThickness="1"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#33000000" BlurRadius="14" ShadowDepth="2" Direction="270"/>
                                    </Border.Effect>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter Margin="0,4"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Heading + export -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Заказы" FontSize="26" FontWeight="Bold"
                       Foreground="{StaticResource HeadingBrush}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Style="{StaticResource SecondaryButton}" MinWidth="140"
                    VerticalAlignment="Center" Command="{Binding ExportCommand}">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <iconPacks:PackIconMaterial Kind="TrayArrowDown" Width="15" Height="15" VerticalAlignment="Center"/>
                    <TextBlock Text="Экспорт" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Status filter dropdown (replaces the chip row from the design) -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16" VerticalAlignment="Center">
            <TextBlock Text="Статус" VerticalAlignment="Center" Margin="0,0,10,0"
                       FontSize="13" Foreground="{StaticResource SecondaryTextBrush}"/>
            <ComboBox Style="{StaticResource FilterCombo}" Width="220"
                      ItemsSource="{Binding Filters}" ItemTemplate="{StaticResource ComboItemLabel}"
                      SelectedItem="{Binding SelectedFilter, Mode=TwoWay}"/>
        </StackPanel>

        <!-- Orders table -->
        <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="16" Padding="24,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Column headers -->
                <Grid Grid.Row="0" Margin="0,12,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="*" MinWidth="120"/>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="170"/>
                        <!-- Actions column: 250px when the role can act, collapsed (0) otherwise -->
                        <ColumnDefinition Width="{Binding Data.ShowActions, Source={StaticResource VmProxy}, Converter={StaticResource BoolToGridLengthConverter}, ConverterParameter=250}"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="№" Style="{StaticResource ColHeader}"/>
                    <TextBlock Grid.Column="1" Text="Покупатель" Style="{StaticResource ColHeader}"/>
                    <TextBlock Grid.Column="2" Text="Дата" Style="{StaticResource ColHeader}"/>
                    <TextBlock Grid.Column="3" Text="Позиции" Style="{StaticResource ColHeader}"/>
                    <TextBlock Grid.Column="4" Text="Сумма" Style="{StaticResource ColHeader}"/>
                    <TextBlock Grid.Column="5" Text="Статус" Style="{StaticResource ColHeader}"/>
                </Grid>

                <!-- Empty state: shown instead of the table when nothing matches the current filter -->
                <TextBlock Grid.Row="1" Text="{Binding EmptyText}"
                           FontSize="14" Foreground="{StaticResource SecondaryTextBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,40"
                           Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                              Visibility="{Binding IsEmpty, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <ItemsControl ItemsSource="{Binding Orders}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,1,0,0" Padding="0,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="70"/>
                                            <ColumnDefinition Width="*" MinWidth="120"/>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="170"/>
                                            <ColumnDefinition Width="{Binding Data.ShowActions, Source={StaticResource VmProxy}, Converter={StaticResource BoolToGridLengthConverter}, ConverterParameter=250}"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Style="{StaticResource Cell}"
                                                   Foreground="{StaticResource SecondaryTextBrush}">
                                            <Run Text="#"/><Run Text="{Binding Id, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Grid.Column="1" Text="{Binding CustomerName}" Style="{StaticResource Cell}"
                                                   TextTrimming="CharacterEllipsis" Margin="0,0,12,0"/>
                                        <TextBlock Grid.Column="2" Style="{StaticResource Cell}"
                                                   Foreground="{StaticResource SecondaryTextBrush}"
                                                   Text="{Binding CreatedAt, StringFormat='dd.MM'}"/>
                                        <TextBlock Grid.Column="3" Text="{Binding ItemCount}" Style="{StaticResource Cell}"/>
                                        <TextBlock Grid.Column="4" Style="{StaticResource Cell}"
                                                   Text="{Binding Total, Converter={StaticResource MoneyToRubleConverter}}"/>

                                        <!-- Status badge -->
                                        <Border Grid.Column="5" CornerRadius="10" Padding="10,3"
                                                HorizontalAlignment="Left" VerticalAlignment="Center"
                                                Background="{Binding Status, Converter={StaticResource OrderStatusConverter}, ConverterParameter=Background}">
                                            <TextBlock FontSize="12" FontWeight="SemiBold"
                                                       Text="{Binding Status, Converter={StaticResource OrderStatusConverter}, ConverterParameter=Label}"
                                                       Foreground="{Binding Status, Converter={StaticResource OrderStatusConverter}, ConverterParameter=Foreground}"/>
                                        </Border>

                                        <!-- Row actions: advance to the next state / cancel (role-gated; right margin
                                             keeps the buttons clear of the table's scrollbar) -->
                                        <StackPanel Grid.Column="6" Orientation="Horizontal"
                                                    HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,14,0">
                                            <Button Style="{StaticResource RowAdvanceButton}"
                                                    Content="{Binding AdvanceLabel}"
                                                    Command="{Binding DataContext.AdvanceCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    Visibility="{Binding ShowAdvance, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            <Button Style="{StaticResource RowCancelButton}" Content="Отменить"
                                                    Margin="8,0,0,0"
                                                    Command="{Binding DataContext.CancelOrderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    Visibility="{Binding ShowCancel, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <TextBlock Grid.Row="2" Text="{Binding Error}" Foreground="{StaticResource ErrorBrush}"
                   VerticalAlignment="Bottom" Margin="4"
                   Visibility="{Binding Error, Converter={StaticResource NullToCollapsedConverter}}"/>
    </Grid>
</UserControl>