<Window x:Class="stockfront.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:stockfront.ViewModels"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:LoginViewModel}"
        Title="StockFront — Вход"
        Width="420" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WorkAreaBrush}"
        FontFamily="Segoe UI">

    <!-- Centered card -->
    <Border Background="{StaticResource CardBrush}"
            CornerRadius="20" Margin="28"
            Padding="36,32">
        <Border.Effect>
            <DropShadowEffect Color="#22000000" BlurRadius="24" ShadowDepth="0" />
        </Border.Effect>

        <StackPanel>
            <!-- Brand -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Border Background="{StaticResource AccentBrush}" Width="40" Height="40" CornerRadius="10">
                    <iconPacks:PackIconMaterial Kind="PackageVariantClosed" Foreground="White"
                                                Width="20" Height="20"
                                                HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="StockFront" FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource HeadingBrush}" />
                    <TextBlock Text="складской учёт" FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}" />
                </StackPanel>
            </StackPanel>

            <TextBlock Text="Вход в систему" FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource HeadingBrush}"
                       Margin="0,28,0,4" />
            <TextBlock Text="Введите e-mail и пароль"
                       Foreground="{StaticResource SecondaryTextBrush}" FontSize="13"
                       Margin="0,0,0,20" />

            <TextBlock Text="E-mail" Foreground="{StaticResource SecondaryTextBrush}"
                       FontSize="12" Margin="2,0,0,6" />
            <Border CornerRadius="10" Background="White" BorderBrush="{StaticResource DividerBrush}"
                    BorderThickness="1">
                <TextBox x:Name="EmailBox" BorderThickness="0" Background="Transparent"
                         Height="44" Padding="14,0" VerticalContentAlignment="Center" FontSize="14"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
            </Border>

            <TextBlock Text="Пароль" Foreground="{StaticResource SecondaryTextBrush}"
                       FontSize="12" Margin="2,16,0,6" />
            <Border CornerRadius="10" Background="White" BorderBrush="{StaticResource DividerBrush}"
                    BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <PasswordBox x:Name="PasswordBox" Grid.Column="0" BorderThickness="0" Background="Transparent"
                                 Height="44" Padding="14,0" VerticalContentAlignment="Center" FontSize="14"
                                 PasswordChanged="PasswordBox_OnPasswordChanged" />
                    <TextBox x:Name="PasswordTextBox" Grid.Column="0" BorderThickness="0" Background="Transparent"
                             Height="44" Padding="14,0" VerticalContentAlignment="Center" FontSize="14"
                             IsReadOnly="True" Focusable="False" Visibility="Collapsed" />
                    <Button x:Name="EyeButton" Grid.Column="1" Background="Transparent" BorderThickness="0"
                            Cursor="Hand" Focusable="False" Padding="10,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="EyeButton_PreviewMouseLeftButtonDown"
                            PreviewMouseLeftButtonUp="EyeButton_PreviewMouseLeftButtonUp"
                            MouseLeave="EyeButton_MouseLeave">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="Transparent">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <iconPacks:PackIconMaterial x:Name="EyeIcon" Kind="EyeOutline"
                                                    Foreground="{StaticResource SecondaryTextBrush}"
                                                    Width="20" Height="20" />
                    </Button>
                </Grid>
            </Border>

            <TextBlock Text="{Binding InfoMessage}"
                       Foreground="#3F9D52" FontSize="13"
                       TextWrapping="Wrap" Margin="2,14,0,0"
                       Visibility="{Binding InfoMessage, Converter={StaticResource NullToCollapsedConverter}}" />

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{StaticResource ErrorBrush}" FontSize="13"
                       TextWrapping="Wrap" Margin="2,14,0,0"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

            <Button Content="Войти"
                    Command="{Binding LoginCommand}"
                    IsDefault="True"
                    Height="46" Margin="0,22,0,0"
                    Foreground="White" FontSize="15" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" CornerRadius="12" Background="{StaticResource AccentBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                <TextBlock Text="Нет аккаунта?" Foreground="{StaticResource SecondaryTextBrush}"
                           FontSize="13" VerticalAlignment="Center" />
                <Button Content="Зарегистрироваться" Command="{Binding GoToRegisterCommand}"
                        Margin="6,0,0,0" Background="Transparent" BorderThickness="0" Cursor="Hand"
                        Foreground="{StaticResource AccentBrush}" FontSize="13" FontWeight="SemiBold" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
