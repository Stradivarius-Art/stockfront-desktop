using System.Windows.Controls;

namespace stockfront.Views;

/// <summary>The storefront screen: the product catalog with category chips, the cart overlay and checkout.</summary>
public partial class StorefrontView : UserControl
{
    public StorefrontView() => InitializeComponent();
}