<Window x:Class="stockfront.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:stockfront.ViewModels"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:RegisterViewModel}"
        Title="StockFront — Регистрация"
        Width="420" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WorkAreaBrush}"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="2,14,0,6" />
        </Style>
        <Style x:Key="ErrorText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource ErrorBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin" Value="2,4,0,0" />
        </Style>
        <Style x:Key="InputBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource DividerBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <!-- Hold-to-reveal "eye" button shared by both password fields. The Tag selects the field. -->
        <Style x:Key="EyeButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="EyeButton_PreviewMouseLeftButtonDown" />
            <EventSetter Event="PreviewMouseLeftButtonUp" Handler="EyeButton_PreviewMouseLeftButtonUp" />
            <EventSetter Event="MouseLeave" Handler="EyeButton_MouseLeave" />
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource CardBrush}" CornerRadius="20" Margin="24" Padding="32,26">
        <Border.Effect>
            <DropShadowEffect Color="#22000000" BlurRadius="24" ShadowDepth="0" />
        </Border.Effect>

        <StackPanel>
            <TextBlock Text="Регистрация" FontSize="20" FontWeight="Bold"
                       Foreground="{StaticResource HeadingBrush}" />
            <TextBlock Text="Создайте учётную запись покупателя"
                       Foreground="{StaticResource SecondaryTextBrush}" FontSize="13" Margin="0,4,0,0" />

            <!-- Display name -->
            <TextBlock Text="Имя" Style="{StaticResource FieldLabel}" />
            <Border Style="{StaticResource InputBorder}">
                <TextBox x:Name="DisplayNameBox" Height="42" Padding="14,0" VerticalContentAlignment="Center"
                         FontSize="14" BorderThickness="0" Background="Transparent"
                         Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" />
            </Border>
            <TextBlock Text="{Binding DisplayNameError}" Style="{StaticResource ErrorText}"
                       Visibility="{Binding DisplayNameError, Converter={StaticResource NullToCollapsedConverter}}" />

            <!-- Email (used as the login) -->
            <TextBlock Text="E-mail" Style="{StaticResource FieldLabel}" />
            <Border Style="{StaticResource InputBorder}">
                <TextBox Height="42" Padding="14,0" VerticalContentAlignment="Center" FontSize="14"
                         BorderThickness="0" Background="Transparent"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
            </Border>
            <TextBlock Text="{Binding EmailError}" Style="{StaticResource ErrorText}"
                       Visibility="{Binding EmailError, Converter={StaticResource NullToCollapsedConverter}}" />

            <!-- Password -->
            <TextBlock Text="Пароль" Style="{StaticResource FieldLabel}" />
            <Border Style="{StaticResource InputBorder}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <PasswordBox x:Name="PasswordBox" Grid.Column="0" Height="42" Padding="14,0"
                                 VerticalContentAlignment="Center" FontSize="14" BorderThickness="0"
                                 Background="Transparent" PasswordChanged="PasswordBox_OnPasswordChanged" />
                    <TextBox x:Name="PasswordTextBox" Grid.Column="0" Height="42" Padding="14,0"
                             VerticalContentAlignment="Center" FontSize="14" BorderThickness="0"
                             Background="Transparent" IsReadOnly="True" Focusable="False" Visibility="Collapsed" />
                    <Button Grid.Column="1" Style="{StaticResource EyeButton}" Tag="Password">
                        <iconPacks:PackIconMaterial x:Name="PasswordEyeIcon" Kind="EyeOutline"
                                                    Foreground="{StaticResource SecondaryTextBrush}"
                                                    Width="20" Height="20" />
                    </Button>
                </Grid>
            </Border>
            <TextBlock Text="{Binding PasswordError}" Style="{StaticResource ErrorText}"
                       Visibility="{Binding PasswordError, Converter={StaticResource NullToCollapsedConverter}}" />

            <!-- Confirm password -->
            <TextBlock Text="Повторите пароль" Style="{StaticResource FieldLabel}" />
            <Border Style="{StaticResource InputBorder}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <PasswordBox x:Name="ConfirmPasswordBox" Grid.Column="0" Height="42" Padding="14,0"
                                 VerticalContentAlignment="Center" FontSize="14" BorderThickness="0"
                                 Background="Transparent" PasswordChanged="ConfirmPasswordBox_OnPasswordChanged" />
                    <TextBox x:Name="ConfirmPasswordTextBox" Grid.Column="0" Height="42" Padding="14,0"
                             VerticalContentAlignment="Center" FontSize="14" BorderThickness="0"
                             Background="Transparent" IsReadOnly="True" Focusable="False" Visibility="Collapsed" />
                    <Button Grid.Column="1" Style="{StaticResource EyeButton}" Tag="Confirm">
                        <iconPacks:PackIconMaterial x:Name="ConfirmPasswordEyeIcon" Kind="EyeOutline"
                                                    Foreground="{StaticResource SecondaryTextBrush}"
                                                    Width="20" Height="20" />
                    </Button>
                </Grid>
            </Border>
            <TextBlock Text="{Binding ConfirmPasswordError}" Style="{StaticResource ErrorText}"
                       Visibility="{Binding ConfirmPasswordError, Converter={StaticResource NullToCollapsedConverter}}" />

            <TextBlock Text="{Binding FormError}" Style="{StaticResource ErrorText}" Margin="2,12,0,0"
                       Visibility="{Binding FormError, Converter={StaticResource NullToCollapsedConverter}}" />

            <Button Content="Зарегистрироваться"
                    Command="{Binding RegisterCommand}" IsDefault="True"
                    Height="46" Margin="0,20,0,0"
                    Foreground="White" FontSize="15" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" CornerRadius="12" Background="{StaticResource AccentBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Content="← Назад ко входу" Command="{Binding BackCommand}"
                    HorizontalAlignment="Center" Margin="0,14,0,0"
                    Background="Transparent" BorderThickness="0" Cursor="Hand"
                    Foreground="{StaticResource SecondaryTextBrush}" FontSize="13" />
        </StackPanel>
    </Border>
</Window>
